@model IEnumerable<Book_Store_Memoir.Models.Models.Orders>

@{
    ViewData["Title"] = "Index";

}
<div class="page-header">

    <div class="header-sub-title">
        <nav class="breadcrumb breadcrumb-dash">
            <a asp-area="Admin" asp-controller="" asp-action="Index" class="breadcrumb-item"><i class="anticon anticon-home m-r-5"></i>Home</a>
            <span class="breadcrumb-item active">Danh sách đơn hàng: </span>
        </nav>
    </div>
</div>
<div class="form-group">

    @*<form method="post" id="myForm">
    <label>Chọn trạng thái đơn hàng</label>
    <select class="form-control" id="statusID" name="statusID" asp-items="@ViewBag.DSTT" required onchange="submitForm()">
    <option value="0" disabled selected>Chọn trạng thái đơn hàng</option>
    </select>
    </form>*@

    @*   <form method="get">
    <label for="brand-select">Choose a brand:</label>
    <select id="statusID" name="statusID" asp-items="ViewBag.DSTT">
    <option value="" disable selected>Chọn trạng thái đơn hàng</option>
    </select>
    <button type="submit">Filter</button>
    </form>*@
    @*<form method="post" id="myForm">
    <label>Chọn trạng thái đơn hàng</label>
    <select class="form-control" id="statusID" name="statusID" asp-items="@ViewBag.DSTT" required onchange="submitForm()">
    <option value="0" disabled selected>Chọn trạng thái đơn hàng</option>
    </select>
    </form>*@
@*    <a class="btn btn-secondary btn-tone m-r-5" asp-area="Admin" asp-controller="Order" asp-action="ListOrderConfirm">Đơn hàng đã xác nhận</a>
*@
</div>
<table class="table" id="list-order">
    <thead>
        <tr>
            <th>
                Mã Đơn Hàng
            </th>
            <th>
                Tên Khách Hàng
            </th>
            @*   <th>
            Ngày mua
            </th>*@
            <th>
                Tổng tiền
            </th>
            <th>
                Trạng Thái
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Id)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Customers.Name)
                </td>
                <td>
                    @item.TotalAmount.Value.ToString("#,##0") VNĐ
                </td>

                <td>
                    @item.OrderStatus.Status
                </td>
                <td>
                    <a class="btn btn-primary btn-tone m-r-5" asp-area="Admin" asp-controller="Order" asp-action="Details" asp-route-id="@item.Id">View</a>
                    @*                    <a class="btn btn-secondary btn-tone m-r-5" asp-area="Admin" asp-controller="Order" asp-action="ChangeStatus" asp-route-id="@item.Id">Chỉnh sửa trạng thái</a>
                *@
                </td>
                <td>
                </td>


            </tr>
        }
    </tbody>
</table>



<script>
    function submitForm() {
        document.getElementById("myForm").submit();
    }
</script>