@model Book_Store_Memoir.Models.Publisher
@using AspNetCoreHero.ToastNotification.Abstractions
@{
    ViewData["Title"] = "Details";
}

<div class="card-body">
    @if ((int)ViewBag.flag == 0)
    {
        <h3>Bạn có thực sự muốn xóa nhà xuất bản này?</h3>
        <div class="alert alert-warning" role="alert">
            <p class="mb-0">Hành động này sẽ xóa nhà xuất bản "<strong>@Model.Name</strong>". Bạn chắc chắn chứ?</p>
        </div>
        <div class="d-flex justify-content-end">
            <a asp-action="Index" class="btn btn-secondary mr-2">Hủy Bỏ</a>
            <form asp-action="Delete" asp-route-id="@Model.Id" class="d-inline">
                <input type="submit" value="Xóa" class="btn btn-danger" />
            </form>
        </div>
    }
    else
    {
        <div class="alert alert-danger" role="alert">
            <p class="mb-0">Bạn không thể xóa nhà xuất bản này vì hiện tại đã có sản phẩm trong nhà xuất bản này.</p>
        </div>
    }

    <h4 class="card-title mt-4">Thông tin nhà xuất bản: @Model.Name</h4>
    <div class="table-responsive">
        <table class="product-info-table m-t-20 table">
            <tbody>
                <tr>
                    <td>ID:</td>
                    <td class="text-dark font-weight-semibold">@Model.Id</td>
                </tr>
                <tr>
                    <td>Tên nhà xuất bản:</td>
                    <td>@Model.Name</td>
                </tr>
            </tbody>
        </table>
    </div>
</div>
